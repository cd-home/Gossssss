package main

func main() {
	/*
		1. 反射: 能在运行时获取变量的类型、值；以及更新其值；调用其方法；执行内置操作等.
		2. 反射依赖接口 interface{} == any
		3. 核心API reflect.TypeOf reflect.ValueOf
		4. 核心类型 reflect.Type reflect.Value
		5. 一个变量就是一个可寻址的内存空间，里面存储了一个值，并且存储的值可以通过内存地址来更新
			提供了很多的API,来获取和设置值

		6. 注意:
			反射的可读性不高, 并且降低了程序的安全性, 无法做静态类型检查, 反射降低程序性能.
	*/
}
